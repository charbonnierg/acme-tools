"""The `__about__` module exposes the version of the `acme_tools` package:

Example:

```python
from acme_tools.__about__ import __version__
print(__version__)
```

Note that container version can also be imported directly from `acme_tools` package:

Example:

```python
from acme_tools import __version__
print(__version__)
```
"""
__version__ = "0.1.0"
